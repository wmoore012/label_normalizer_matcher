[pytest]
addopts = -q --strict-markers --maxfail=1 --cov=label_normalizer_matcher --cov-report=term-missing
testpaths = tests
timeout = 30
markers =
    benchmark: marks tests as benchmarks (deselect with '-m "not benchmark"')